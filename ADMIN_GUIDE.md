# 管理员操作手册

## 一、角色权限说明

| 角色 | 说明 |
|------|------|
| owner | 超级管理员，可添加/禁用其他管理员，可执行所有操作 |
| admin | 普通管理员，可编辑比分、射手榜、红黄牌、公告解析、备份恢复 |

- 观众：无需登录，仅可浏览数据（只读）
- 管理员：登录后可编辑所有数据

## 二、创建新管理员账号

### 步骤 1：在 Supabase 创建用户

1. 登录 [supabase.com](https://supabase.com) 进入项目后台
2. 进入 `Authentication -> Users`
3. 点击 `Add user`，输入邮箱和初始密码

### 步骤 2：授予管理员权限

**方式 A：网页操作（推荐）**

1. 使用 owner 账号登录网站
2. 展开"管理员入口" → "管理员管理"
3. 输入新管理员邮箱，点击"添加管理员"

**方式 B：SQL 操作**

```sql
insert into public.admin_users(email, role, is_active)
values ('new-admin@example.com', 'admin', true)
on conflict (email) do update set is_active = excluded.is_active;
```

如需创建超级管理员，将 role 改为 `'owner'`。

## 三、密码管理

### 管理员自己修改密码

1. 登录后展开"管理员入口" → "密码管理"
2. 输入新密码（至少6位）
3. 点击"修改当前账号密码"

### 发送重置密码邮件给其他人

1. 登录后展开"管理员入口" → "密码管理"
2. 在"重置密码邮箱"输入对方邮箱（留空则发给当前账号）
3. 点击"发送重置密码邮件"

对方收到邮件后，点击链接跳转到密码重置页面。

## 四、禁用/恢复管理员

### 网页操作

在"管理员管理"表格中找到对应账号，点击"禁用"或"启用"。

> 注意：不能禁用当前登录的自己。

### SQL 操作

```sql
-- 禁用
update public.admin_users set is_active = false where email = 'someone@example.com';

-- 恢复
update public.admin_users set is_active = true where email = 'someone@example.com';
```

## 五、日常操作流程

### 1. 赛前确认赛程

检查"接下来赛事预告"中的未开赛场次是否正确。

### 2. 赛后录入比分

两种方式：

**手动录入**
- 在"比赛赛果"区块找到对应场次
- 输入主队和客队得分，点击"保存"

**公告解析**
- 将当天赛事公告全文粘贴到"公告自动解析"文本框
- 点击"解析并覆盖更新"
- 系统自动提取：比分、进球队员、红黄牌记录

> 解析时不会清空未提及的场次，为增量更新。如需对同一公告重新解析，请勾选"强制重新解析"。

### 3. 淘汰赛阶段

淘汰赛开始前，系统根据小组赛积分自动生成对阵图（A1 vs B4 等）。

管理员在"淘汰赛比分录入"区块逐场录入：
- 常规时间比分
- 点球大战比分（如有）

### 4. 数据备份

每次大规模更新前：
1. 点击"导出备份 JSON"下载备份文件
2. 保存到本地备用

如需恢复：选择备份 JSON 文件，点击确认即可覆盖当前数据。

## 六、故障排查

### 页面显示"只读浏览模式"

- **本地模式**：默认就是可编辑的，正常使用
- **云端模式**：需要用管理员账号登录后才可编辑。展开"管理员入口"登录。

### 数据保存失败

1. 检查网络连接
2. 尝试"从云端重新读取"同步最新状态
3. 如仍异常，使用之前导出的备份文件恢复

### 公告解析结果不对

- 检查公告格式是否为标准格式（球队名: 比分 进球队员...）
- 尝试勾选"强制重新解析"
- 部分球员名可能因特殊字符无法识别，可手动补充录入

### 忘记管理员密码

在登录页使用"忘记密码"功能，Supabase 会发送重置邮件到账号邮箱。

## 七、安全建议

1. **不要**在公开场合展示管理员账号密码
2. 多人管理时建议创建多个 admin 账号，而非共用 owner 账号
3. 定期导出备份，防止数据丢失
4. 禁用离任管理员的账号权限