# Football Live Board — Cursor 上下文摘要（续作必读）

> **在新对话恢复进度**：请读取本文件 `/.cursor_context.md`。你之前正在协助开发「学院杯足球赛事实时积分榜」静态前端 + Supabase 后端项目，请从本文档记录的进度点继续。

---

## 1. 项目定位与目标

- **产品**：足球小组赛（A/B 组）实时积分榜、射手榜、红黄牌榜、未开赛预告；支持手动录比分、公告文本自动解析导入；后续可扩展淘汰赛。
- **权限模型**：**观众匿名只读**；**管理员登录后可写**；**Owner（超级管理员）** 可管理 `admin_users`（增删启用/禁用），普通 **admin** 只能改赛事数据。
- **部署**：静态站点（Netlify Drop 等）+ **Supabase**（Postgres + Auth + RLS）。

---

## 2. 仓库 / 目录结构（项目根：`football-live-board/`）

| 路径 | 说明 |
|------|------|
| `index.html` | 单页：积分榜、赛果录入、公告解析、备份、预告、射手/红黄牌、管理员入口（登录、密码管理、管理员管理） |
| `app.js` | 全部业务逻辑：CONFIG 赛程、状态、渲染、Supabase 读写、公告解析、备份、权限 |
| `styles.css` | 样式（含 admin-only、卡片、表格、表单） |
| `assets/sufa-logo.png` | 页头 Logo |
| `supabase-schema.sql` | 建表 + RLS + `is_admin` / `is_owner` 函数 + 初始 owner 插入 |
| `README.md` | 简要部署与 Supabase 说明 |
| `ADMIN_GUIDE.md` | 管理员注册、密码、禁用等操作手册（给用户） |
| `.cursor_context.md` | **本文件**：技术决策与续作说明 |

---

## 3. 重要技术决策（摘要）

### 3.1 为何纯前端 + Supabase

- 无需自建 Node 服务；静态托管即可公网访问。
- **RLS** 在数据库层限制写入，anon key 放前端仍可「公开读、管理员写」。

### 3.2 数据存储分层

- **Auth**：Supabase `Authentication -> Users` — 登录邮箱+密码；与业务表分离。
- **业务表**：`matches`, `scorers`, `cards` — 赛程比分与榜单。
- **权限表**：`admin_users(email, role, is_active)` — `role` ∈ `owner` | `admin`；仅 **owner** 可 `INSERT/UPDATE/DELETE` 本表（通过 `is_owner()` 策略）。

### 3.3 RLS 与递归问题（已解决）

- 早期在 `admin_users` 策略里直接 `EXISTS (SELECT ... FROM admin_users ...)` 会触发 **infinite recursion**。
- **方案**：`SECURITY DEFINER` 函数 `public.is_admin(text)`、`public.is_owner(text)`，策略里只调用函数，避免策略自引用递归。
- `matches/scorers/cards` 的写策略：`using (public.is_admin(auth.jwt()->>'email'))`。

### 3.4 旧表迁移：`admin_users.role`

- 若库中已有无 `role` 列的旧表，脚本含：`ALTER TABLE ... ADD COLUMN IF NOT EXISTS role` + `UPDATE ... SET role='admin' WHERE role IS NULL`。
- 初始 owner：`w2564139064@163.com`（在 SQL 中 `INSERT ... ON CONFLICT` 更新）；**勿把真实密钥写进公开仓库**。

### 3.5 云端同步策略（scorers/cards）

- **问题**：仅用 `upsert` 不会在本地清空后删除云端多余行。
- **方案**：保存时 `matches` 仍 `upsert`；`scorers` / `cards` 采用 **先 `delete` 全表再 `insert` 当前快照**（在 `saveCloudAll` 中），保证清空与去重后云端一致。

### 3.6 去重与公告重复点击

- `dedupeScorers` / `dedupeCards`：按 `team|player` 合并；合并时用 **max** 避免历史重复行导致数字异常放大（与「累计」语义需权衡：当前设计偏防误重复导入）。
- **同一份公告文本**：`hashText` + `lastNoticeHash` 存 localStorage；完全相同文本第二次解析会拦截，防误点翻倍。
- **解析语义**：`parseNotice` 为**增量**：不清空未在公告中出现的场次；只更新解析到的比分与累计射手/黄牌（需注意与「完全重算榜单」的语义差异）。

### 3.7 前端权限判断

- `refreshAuth`：读 session 邮箱后查 `admin_users`（`is_active=true`）得 `role`；`canEdit = !!row`；`canManageAdmins = (role === 'owner')`。
- 若 `admin_users` 查询失败：**回退** `ADMIN_EMAILS` 数组（`app.js` 顶部），主要用于本地/调试。
- **本地模式**（未配 Supabase）：`canEdit=true`，视为本地管理员（仅本地文件打开时）。

### 3.8 事件绑定健壮性

- `bindIfExists(selector, event, handler)`：DOM 节点不存在时不抛错，避免线上 HTML 与 `app.js` 版本不一致导致整页脚本中断（曾导致「登录无反应」）。

### 3.9 密码功能（Supabase Auth）

- 登录后：`updateUser({ password })` 修改当前账号密码。
- `resetPasswordForEmail(email, { redirectTo })`：`redirectTo` 设为当前页 `origin + pathname`，用户从邮件链接回站点后可在同域完成重置（若邮件模板未配好可能失败）。

### 3.10 Netlify 与国内网络

- `*.netlify.app` 为境外；部分运营商/地区访问不稳定，**校园网有时反而能访问**属常见现象。
- **待办/建议**：自定义域名、或国内 CDN/镜像说明可写进 `README`。

---

## 4. 代码锚点（按功能）

| 功能 | 主要位置 |
|------|-----------|
| Supabase URL / Key / 白名单回退 | `app.js` 顶部 `SUPABASE_*`, `ADMIN_EMAILS` |
| 赛程与分组 | `CONFIG` in `app.js` |
| 积分榜计算 | `teamStats`, `renderStandings` |
| 赛果行渲染与保存 | `renderMatches`, `#matchInputList` click |
| 公告解析 | `parseNotice`, `normalizeText`, `upsertScorer`, `upsertCard`；`#forceParseNotice` 强制跳过同文 hash 拦截 |
| 云端读 | `loadCloudData`（matches 与 `CONFIG.matches` 合并） |
| 云端写 | `saveCloudAll` |
| 备份导出/导入 | `getBackupPayload`, `downloadJson`, `applyBackupPayload`, `#exportBackupBtn` 等 |
| 管理员表加载 | `loadAdminUsers`, `renderAdminUsers` |
| Owner 专属 UI | `updateModeUI` 中隐藏 `#adminManageForm` 若非 owner；表格禁用按钮 |
| 密码表单 | `index.html` `#changePasswordForm`, `#resetPasswordForm`；`app.js` 对应 submit |
| 射手/红牌删除 | 表格 `#scorersTable` / `#cardsTable`；**快速删除** `#quickDeleteScorerForm`, `#quickDeleteCardForm` |
| 样式与 admin-only | `styles.css` `.hidden`, `.admin-only`（由 JS `updateModeUI` 切换） |

---

## 5. 数据库对象（`supabase-schema.sql`）

- **表**：`matches`, `scorers`, `cards`, `admin_users`
- **函数**：`public.is_admin(user_email text)`, `public.is_owner(user_email text)` — `SECURITY DEFINER`, `search_path = public`
- **策略要点**：
  - `matches/scorers/cards`：`SELECT` 公开；`ALL` 写入需 `is_admin(jwt email)`
  - `admin_users`：`SELECT` 给 `authenticated`；`ALL` 维护仅 `is_owner(jwt email)`
- **执行顺序**：若报错列不存在，先跑迁移段再跑全量；网络错误 `api.supabase.com` 多为本地网络/代理问题。

---

## 6. 运维与用户流程（简）

1. **首次**：Supabase 执行 `supabase-schema.sql`；Auth 创建用户；`admin_users` 中保证至少一个 `owner`。
2. **加管理员**：Owner 在网页「管理员管理」添加邮箱；同时在 Auth 中创建该用户并设初始密码或发重置邮件。
3. **日常更新**：管理员登录 → 粘贴公告解析（或手改）→ 导出 JSON 备份。
4. **部署**：将项目文件夹拖到 Netlify；确保线上 `app.js` 含 Supabase 配置，与本地一致。

---

## 7. 已知问题与风险

- **国内访问 Netlify 不稳定**：非代码 bug；需网络或域名/CDN 方案。
- **公告解析**：格式依赖正则；队名需与 `CONFIG` 或 `TEAM_ALIASES` 一致。
- **同公告 hash 拦截**：已支持勾选「强制重新解析」跳过 hash 检查；默认仍防误点翻倍。
- **去重用 max**：若业务上要「同球员多次公告累计进球」且允许多行，需重新设计合并规则（**待办/产品确认**）。

---

## 8. 待办事项（建议优先级）

| 优先级 | 事项 |
|--------|------|
| P1 | 重新部署 Netlify，保证线上 `index.html` / `app.js` / `styles.css` 与仓库一致 |
| P1 | 敏感信息：`SUPABASE_ANON_KEY` 等若需开源，改为构建时注入或环境变量（当前写死在 `app.js`） |
| P2 | ~~公告解析：可选「强制重新解析」开关~~（已实现：`#forceParseNotice`） |
| P2 | 访问性：国内用户说明 + 自定义域名文档 |
| P2 | 淘汰赛阶段：`CONFIG.phases` 或新表 + 单独 UI |
| P3 | 操作审计表（谁何时改了什么） |
| P3 | 实时订阅（Supabase Realtime）自动刷新观众端 |

---

## 9. 恢复对话用提示语（复制到新窗口）

```
请读取项目根目录的 .cursor_context.md 文件。你之前正在协助开发 football-live-board（足球赛事实时积分榜 + Supabase + Netlify），这是详细技术摘要。请从该文档记录的进度与待办继续，不要重复已完成的架构决策；修改代码时优先保持与现有 app.js / supabase-schema.sql 一致。
```

---

## 10. 文件版本提示

- 本文档生成于对话收尾阶段；若之后代码有变，请同步更新 `.cursor_context.md` 中的「代码锚点」「待办」「已知问题」三节。

---

*End of .cursor_context.md*
